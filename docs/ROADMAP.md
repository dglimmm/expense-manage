# 개인 비용 관리 사이트 개발 로드맵

Notion을 CMS로 활용해 별도 백엔드 없이 비용을 관리하고, 기존 엑셀 양식 그대로 다운로드하는 개인 비용 관리 사이트

## 개요

개인 비용 관리 사이트는 **개인 사용자**를 위한 **노코드 비용 관리 솔루션**으로 다음 기능을 제공합니다:

- **Notion 데이터베이스 연동**: Notion에 지출 항목을 작성하면 별도 관리자 페이지 없이 자동으로 사이트에 반영
- **날짜별 필터링**: 연/월/일 단위로 원하는 기간의 지출 목록을 조회
- **엑셀 다운로드**: 필터링된 목록을 기존에 사용하던 엑셀 양식(서식·수식·차트 보존)에 채워 넣어 다운로드
- **반응형 디자인**: 모바일/태블릿/데스크톱 모든 환경에서 사용 가능한 UI

## 개발 워크플로우

1. **작업 계획**

- 기존 코드베이스를 학습하고 현재 상태를 파악
- 새로운 작업을 포함하도록 `ROADMAP.md` 업데이트
- 우선순위 작업은 마지막 완료된 작업 다음에 삽입

2. **작업 생성**

- 기존 코드베이스를 학습하고 현재 상태를 파악
- `/tasks` 디렉토리에 새 작업 파일 생성
- 명명 형식: `XXX-description.md` (예: `001-setup.md`)
- 고수준 명세서, 관련 파일, 수락 기준, 구현 단계 포함
- API/비즈니스 로직 작업 시 "## 테스트 체크리스트" 섹션 필수 포함 (Playwright MCP 테스트 시나리오 작성)
- 예시를 위해 `/tasks` 디렉토리의 마지막 완료된 작업 참조. 예를 들어, 현재 작업이 `012`라면 `011`과 `010`을 예시로 참조.
- 이러한 예시들은 완료된 작업이므로 내용이 완료된 작업의 최종 상태를 반영함 (체크된 박스와 변경 사항 요약). 새 작업의 경우, 문서에는 빈 박스와 변경 사항 요약이 없어야 함. 초기 상태의 샘플로 `000-sample.md` 참조.

3. **작업 구현**

- 작업 파일의 명세서를 따름
- 기능과 기능성 구현
- API 연동 및 비즈니스 로직 구현 시 Playwright MCP로 테스트 수행 필수
- 각 단계 후 작업 파일 내 단계 진행 상황 업데이트
- 구현 완료 후 Playwright MCP를 사용한 E2E 테스트 실행
- 테스트 통과 확인 후 다음 단계로 진행
- 각 단계 완료 후 중단하고 추가 지시를 기다림

4. **로드맵 업데이트**

- 로드맵에서 완료된 작업을 ✅로 표시

## 개발 단계

### Phase 1: 애플리케이션 골격 구축 ✅

- **Task 001: 프로젝트 구조 및 라우팅 설정** ✅ - 완료
  - ✅ 스타터킷 보일러플레이트 정리 (로그인/회원가입/랜딩 섹션 등 불필요 코드 제거)
  - ✅ Next.js App Router 기반 라우트 구조 생성 (`/` 홈 대시보드, `/expenses` 날짜 필터링 화면)
  - ✅ 비용 다운로드용 Route Handler 골격 생성 (`app/api/export/route.ts` 빈 핸들러)
  - ✅ 공통 레이아웃 컴포넌트 골격 구현 (헤더/푸터)
  - ✅ Lucide React 아이콘 및 기본 메타데이터 설정

- **Task 002: 타입 정의 및 Notion 데이터 모델 설계** ✅ - 완료
  - ✅ Notion 데이터베이스 필드 기반 TypeScript 인터페이스 정의 (`content`, `category`, `useDate`, `cost`)
  - ✅ 비용 항목(`Expense`) 및 목록/필터 타입 정의 (`src/types/expense.ts`)
  - ✅ Notion API 응답 → 도메인 모델 변환 타입 정의
  - ✅ 엑셀 셀 매핑 계약 타입 정의 (데이터 시작 셀, 컬럼-필드 매핑 — PRD 8.3절 기반)
  - ✅ 날짜 필터(연/월/일) 파라미터 타입 정의

### Phase 2: UI/UX 완성 (더미 데이터 활용) ✅

- **Task 003: 공통 컴포넌트 라이브러리 및 더미 데이터 구성** ✅ - 완료
  - ✅ shadcn/ui 기반 공통 컴포넌트 설치 및 구성 (table, button, select, card, badge 등)
  - ✅ 디자인 시스템 및 스타일 가이드 적용 (Tailwind CSS v4)
  - ✅ 더미 비용 데이터 생성 및 관리 유틸리티 작성 (`src/lib/mock/expenses.ts`)
  - ✅ 비용 포맷 유틸리티 작성 (통화 표시, 날짜 포맷)

- **Task 004: 모든 페이지 UI 완성 (더미 데이터)** ✅ - 완료
  - ✅ 홈 대시보드 UI 구현 (연간 지출 내역 요약, 더미 데이터 사용)
  - ✅ 날짜 필터링 화면 UI 구현 (기간 선택 컨트롤 + 비용 목록 테이블)
  - ✅ 엑셀 다운로드 버튼 UI 배치 (동작은 Phase 3에서 연결)
  - ✅ 반응형 디자인 및 모바일 최적화 (테이블/카드 레이아웃 전환)
  - ✅ 로딩/빈 상태(empty state) UI 처리

### Phase 3: 핵심 기능 구현

- **Task 005: Notion 연동 준비 및 환경 설정** - 우선순위
  - `@notionhq/client` 패키지 설치
  - Notion 데이터베이스 생성 (`content`, `category`, `useDate`, `cost` 필드)
  - Notion Integration 생성 및 API 키 발급, 데이터베이스 연결(공유) 설정
  - 환경 변수 설정 (`NOTION_API_KEY`, `NOTION_DATABASE_ID`) 및 `.env.example` 문서화
  - 환경 변수 검증 유틸리티 작성

- **Task 006: 비용 목록 조회 및 날짜 필터링 구현**
  - Notion API 클라이언트 초기화 및 데이터베이스 쿼리 함수 작성 (`src/lib/notion/queries.ts`)
  - Notion 응답을 도메인 모델(`Expense`)로 변환하는 매퍼 구현
  - 날짜별(연/월/일) 필터링 로직 구현 (Notion `filter` 또는 서버 사이드 필터)
  - 더미 데이터를 실제 Notion API 호출로 교체
  - 홈 대시보드 및 필터링 화면에 실제 데이터 연동
  - Playwright MCP를 활용한 데이터 조회/필터링 E2E 테스트 수행

- **Task 007: 엑셀 다운로드 기능 구현**
  - `xlsx-populate` 라이브러리 설치 (PRD 7-4-1)
  - 엑셀 템플릿 파일 배치: `src/lib/templates/expense-template.xlsx` (서버 전용 경로, `/public` 미사용 — PRD 8.1)
  - 템플릿 파일을 서버(Route Handler)에서 로드하는 로직 구현 (PRD 7-4-2)
  - 셀 매핑 계약 정의 및 적용: 데이터 시작 셀 `A5`, 컬럼 매핑 A:`useDate`, B:`category`, C:`content`, D:`cost` (PRD 8.3 — 실제 양식에 맞게 확정)
  - 헤더/합계 등 정적 영역은 보존하고 데이터 영역만 기록 (서식·수식·차트 유지)
  - 필터링된 데이터를 셀에 기록하고 워크북을 버퍼로 생성 (PRD 7-4-4)
  - `Content-Disposition` 헤더와 함께 바이너리 다운로드 응답 반환 (PRD 7-4-5)
  - `next.config.ts`의 `outputFileTracingIncludes`로 템플릿 파일을 함수 번들에 명시적 포함 (Vercel 배포 대응 — PRD 8.2)
  - 다운로드 버튼과 API 연결 및 현재 필터 조건 전달
  - Playwright MCP로 다운로드 플로우 E2E 테스트 (파일 생성/응답 헤더/셀 매핑 검증)

- **Task 007-1: 핵심 기능 통합 테스트**
  - Playwright MCP를 사용한 전체 사용자 플로우 테스트 (조회 → 필터링 → 다운로드)
  - Notion API 연동 및 날짜 필터링 비즈니스 로직 검증
  - 엣지 케이스 테스트 (빈 결과, 잘못된 날짜 범위, Notion API 오류, 누락 필드)
  - 엑셀 다운로드 결과의 셀 매핑 정확성 및 양식 보존 검증

### Phase 4: 고급 기능 및 최적화

- **Task 008: 스타일링 다듬기 및 사용자 경험 향상**
  - UI 최종 다듬기 및 디자인 일관성 점검
  - 반응형 대응 최종 검증 (모바일/태블릿/데스크톱)
  - 다운로드/조회 진행 상태 피드백(로딩, 토스트) 개선
  - 접근성(a11y) 및 키보드 내비게이션 점검

- **Task 009: 성능 최적화 및 배포**
  - Notion 데이터 캐싱 전략 적용 (Next.js `revalidate`/`fetch` 캐시)
  - 서버 컴포넌트/번들 최적화 (엑셀 라이브러리 클라이언트 번들 제외 확인)
  - Vercel 배포 및 환경 변수/템플릿 파일 번들 포함 검증
  - 모니터링 및 에러 로깅 구성

## Out of Scope (MVP 제외 — 향후 고려 사항)

> PRD 9절 기준. MVP 범위에 포함하지 않으며, 필요 시 별도 Phase로 승격하여 진행합니다.

- **지출 항목 CRUD**: 사이트에서 지출 항목 직접 추가/수정/삭제 (현재는 Notion에서만 관리)
- **카테고리별 통계/시각화**: 카테고리별 집계, 차트/그래프 대시보드
- **다중 사용자 지원 및 인증**: 사용자 계정, 권한, 로그인 시스템
- **Notion API 페이지네이션/rate limit 고도화**: 데이터 양 증가 시 `start_cursor` 기반 페이지네이션 및 rate limit 대응 적용 검토
